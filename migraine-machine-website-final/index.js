// window.location.reload()

$( function() {
    $( "#one" ).draggable();
    $( "#ten" ).droppable({
      drop: function( event, ui ) {
        ui.draggable.hide();
      }
    });
  } );


$( function() {
    $( "#two" ).draggable();
    $( "#ten" ).droppable({
      drop: function( event, ui ) {
        ui.draggable.hide();
      }
    });
  } );


$( function() {
    $( "#three" ).draggable();
    $( "#ten" ).droppable({
      drop: function( event, ui ) {
        ui.draggable.hide();
      }
    });
  } );


$( function() {
    $( "#four" ).draggable();
    $( "#ten" ).droppable({
      drop: function( event, ui ) {
        ui.draggable.hide();
      }
    });
  } );


$( function() {
    $( "#five" ).draggable();
    $( "#ten" ).droppable({
      drop: function( event, ui ) {
        ui.draggable.hide();
      }
    });
  } );


$( function() {
    $( "#six" ).draggable();
    $( "#ten" ).droppable({
      drop: function( event, ui ) {
        ui.draggable.hide();
      }
    });
  } );


$( function() {
    $( "#seven" ).draggable();
    $( "#ten" ).droppable({
      drop: function( event, ui ) {
        ui.draggable.hide();
      }
    });
  } );


$( function() {
    $( "#eight" ).draggable();
    $( "#ten" ).droppable({
      drop: function( event, ui ) {
        ui.draggable.hide();
      }
    });
  } );


$( function() {
    $( "#nine" ).draggable();
    $( "#ten" ).droppable({
      drop: function( event, ui ) {
        ui.draggable.hide();
      }
    });
  } );